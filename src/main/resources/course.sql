CREATE TABLE `spring_boot_demo`.`course` (
                                             `id` INT(11) NOT NULL,
                                             `name` VARCHAR(45) NULL,
                                             `duration` INT NULL,
                                             PRIMARY KEY (`id`));
